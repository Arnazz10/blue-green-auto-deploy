apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
spec:
  selector:
    app: sample-app
    version: blue # This selector will switch between blue and green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
